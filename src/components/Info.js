import React, { useState, useEffect } from "react";

const Info = (props) => {
    const { item } = props;
    const bigImg = item.card_images[0].image_url;
    return (
        <div className="info">
            
        </div>
    );
};

export default Info;